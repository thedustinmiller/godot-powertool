# Managed by `cargo xtask update`. Edit [source].url or [source].ref
# to change where powertool is pulled from. The [applied] block is
# rewritten by the updater after a successful sync — it's diagnostic
# provenance, not configuration.

schema_version = 1

[source]
url = "ssh://git@git.rcr.pub/dustin/godot-powertool.git"
ref = "50d9f42dd5f67cc845d627639a82ca644ea89899"
